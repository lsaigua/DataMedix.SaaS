@* Se muestra a un usuario autenticado que no tiene el permiso de la página. *@

<div class="ad-wrap">
    <div class="ad-icon">
        <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none"
             stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="11" width="18" height="11" rx="2" />
            <path d="M7 11V7a5 5 0 0 1 10 0v4" />
        </svg>
    </div>
    <h1 class="ad-title">No tiene acceso a esta sección</h1>
    <p class="ad-text">
        Su rol no incluye el permiso necesario para ver esta página.
        Si cree que debería tenerlo, solicítelo al administrador de su centro:
        puede habilitarlo desde <strong>Roles y permisos</strong>.
    </p>
    <a href="/dashboard" class="ad-btn">Volver al inicio</a>
</div>

<style>
    .ad-wrap {
        max-width: 460px;
        margin: 4rem auto;
        text-align: center;
        padding: 2rem 1.5rem;
    }
    .ad-icon {
        width: 62px;
        height: 62px;
        margin: 0 auto 1.1rem;
        border-radius: 16px;
        background: #fef2f2;
        color: #dc2626;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .ad-icon svg { width: 30px; height: 30px; }
    .ad-title { font-size: 1.2rem; font-weight: 800; color: #0f172a; margin: 0 0 0.6rem; }
    .ad-text { font-size: 0.88rem; color: #64748b; line-height: 1.6; margin: 0 0 1.4rem; }
    .ad-btn {
        display: inline-block;
        padding: 0.55rem 1.2rem;
        background: #0ea5e9;
        color: white;
        border-radius: 8px;
        font-size: 0.87rem;
        font-weight: 600;
        text-decoration: none;
    }
    .ad-btn:hover { background: #0284c7; }
</style>
