# Build artifacts del host — NO copiar al contenedor
**/bin/
**/obj/

# Salidas de publish — 52 MB de DLLs que no deben viajar al contexto de build
publish/
publish_out/

# IDE
.vs/
.vscode/
.idea/

# Git
.git/

# PROD credentials nunca en imagen — usa Railway env vars
# DEV y QA van embebidos en imagen y se activan via ASPNETCORE_ENVIRONMENT
**/appsettings.Production.json

# Docker mismo
Dockerfile
.dockerignore
